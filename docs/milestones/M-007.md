# Milestone M-007

## Source
- Issue: #8
- Area: area/ui

## Acceptance Criteria
1. `openapi/mvp.yaml` exists and defines MVP endpoints/schemas for `/v1/models`, `/api/keys`, `/api/policies`.
2. Repo provides deterministic commands to validate OpenAPI and generate TypeScript typed artifacts/mocks.
3. Generated artifacts are committed and CI checks they stay in sync with the OpenAPI source.
4. Generated mocks/examples are available for the key endpoints and compile under TypeScript.

## Scope
- In scope: OpenAPI contract, type generation, mock generation, CI sync check, milestone docs.
- Out of scope: SDK publishing, runtime route implementation for all documented endpoints.

## Test Plan
- Run `npm run openapi:validate`.
- Run `npm run openapi:generate`.
- Run `make ci` (includes `openapi:check`).

## Verification Commands
```bash
make test
make coverage
make ci
```

## Status
- [x] Implemented
- [x] Tests added/updated
- [x] Gates green
- [ ] PR opened
