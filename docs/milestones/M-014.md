# Milestone M-014

## Source
- Issue: #26
- Area: area/router

## Acceptance Criteria
1. `POST /v1/responses` 提供最小可用 non-streaming stub，返回 OpenAI-compatible 基础字段。
2. 校验失败与不支持模型错误返回统一 error envelope（包含 `code/message/request_id`）。
3. 路由测试覆盖 happy path 与 invalid payload。
4. `make test`, `make coverage`, `make ci` 全部通过，覆盖率满足阈值（global>=85%，key>=80%）。

## Scope
- In scope: `/v1/responses` stub route、schema/contract、最小 provider-adapter 复用、测试。
- Out of scope: streaming responses、tool execution、真实 provider 调用。

## Test Plan
- Integration: `POST /v1/responses` happy path + invalid payload。
- CI: `make test`, `make coverage`, `make ci`。

## Verification Commands
```bash
make test
make coverage
make ci
```

## Status
- [x] Implemented
- [x] Tests added/updated
- [x] Gates green
- [x] PR opened
