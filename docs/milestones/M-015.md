# Milestone M-015

## Source
- Issue: #27
- Area: area/billing

## Acceptance Criteria
1. `/api/orgs/:orgId/usage` 响应新增 `provisional` 与 `finalized_at` 字段，表达数据是否已最终结算。
2. OpenAPI 与生成类型同步包含新增字段，响应结构保持兼容。
3. 覆盖 `group_by` 与 provisional/final 渲染场景的测试通过。
4. `make test`, `make coverage`, `make ci` 全绿且覆盖率达标（global>=85%，key>=80%）。

## Scope
- In scope: usage report response字段扩展、schema/contract同步、测试更新。
- Out of scope: 实时结算、数据库reconciliation逻辑改造。

## Test Plan
- Unit: usage report provisional/final 判定与聚合输出。
- Integration: usage route 在不同时间窗下返回 provisional/finalized_at。
- CI: `make test`, `make coverage`, `make ci`。

## Verification Commands
```bash
make test
make coverage
make ci
```

## Status
- [x] Implemented
- [x] Tests added/updated
- [x] Gates green
- [x] PR opened
