# M-033 /api/models list/filter endpoint

## Status
- in_review

## Scope
- Add optional provider/status filters for `GET /api/models`.
- Keep response schema aligned with OpenAPI and backward-compatible.
- Add integration tests for happy path and invalid query handling.

## Acceptance Criteria
- `GET /api/models` supports optional query filters: `provider`, `status`.
- Response includes capability flags and model status.
- Invalid query values return `400 INVALID_REQUEST`.
- OpenAPI contract updated and generated types synchronized.
- `make test`, `make coverage`, `make ci` pass.

## Validation Plan
- `make test`
- `make coverage`
- `make ci`
