# Milestone M-029

## Source
- Issue: #64
- Area: area/router

## Acceptance Criteria
1. 新增路由决策解释 endpoint，返回 preset 权重与候选过滤结果。
2. trace payload 包含可测试的决策因子（cost/latency/success weights、候选是否被过滤、过滤原因）。
3. trace payload 不暴露敏感信息（authorization/api key/secret）。
4. 测试覆盖 trace schema 与 no-secret 保证。
5. `make test`, `make coverage`, `make ci` 全绿且覆盖率达标。

## Scope
- In scope: `/v1/responses/:responseId/trace`、in-memory trace 存储、OpenAPI/类型同步、测试。
- Out of scope: trace 持久化存储、跨重启查询、完整审计日志系统。

## Verification Commands
```bash
make test
make coverage
make ci
```

## Status
- [x] Implemented
- [x] Tests added/updated
- [x] Gates green
- [x] PR opened
