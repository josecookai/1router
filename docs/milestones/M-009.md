# Milestone M-009

## Source
- Issue: #15
- Area: area/router

## Acceptance Criteria
1. `POST /v1/chat/completions` returns OpenAI-compatible non-streaming `chat.completion` response using stub provider adapter.
2. Request validation accepts MVP fields (`model`, `messages`, `stream=false`, basic sampling fields) and invalid payloads return shared error envelope with `request_id`.
3. Response includes `router` metadata (`provider`, `provider_model`, `request_id`) and `usage`.
4. Tests cover happy path and validation failure path.
5. `make test`, `make coverage`, and `make ci` pass.

## Scope
- In scope: non-streaming chat route, adapter-backed stub response, validation, tests, OpenAPI contract update.
- Out of scope: SSE streaming, failover/retries, tool execution semantics, live provider calls.

## Test Plan
- Unit: provider adapter chat completion stub behavior.
- Integration: `POST /v1/chat/completions` success + invalid request.
- CI: `make test`, `make coverage`, `make ci`.

## Verification Commands
```bash
make test
make coverage
make ci
```

## Status
- [x] Implemented
- [x] Tests added/updated
- [ ] Gates green
- [ ] PR opened
