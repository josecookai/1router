# Milestone M-013

## Source
- Issue: #25
- Area: area/infra

## Acceptance Criteria
1. Worker lifecycle comments are enforced via scripts for start, complete, and blocked flows.
2. PR/worker branch naming is validated against `codex/<lane>-<milestone>-<slug>`.
3. Merge flow removes `in_progress` and `ready` labels when closing issues.

## Scope
- In scope: agent workflow scripts, branch-format validation, blocked helper, merge label cleanup, shell tests.
- Out of scope: changing GitHub workflow definitions or non-agent runtime features.

## Test Plan
- Shell smoke: branch naming validator pass/fail cases.
- Shell smoke: blocked helper dry-run output includes expected label/comment semantics.
- Merge script policy check: verifies closed issue path removes `ready` and `in_progress` labels.

## Verification Commands
```bash
make test
make coverage
make ci
```

## Status
- [x] Implemented
- [x] Tests added/updated
- [x] Gates green
- [x] PR opened
