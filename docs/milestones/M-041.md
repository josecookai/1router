# M-041 Usage/Billing Dashboard APIs

## Status
- in_progress

## Scope
- area/ui
- Control-plane contract stability for usage and billing dashboard listing APIs.

## Acceptance Criteria
1. Add paginated `GET /api/usage` control-plane endpoint with stable cursor semantics and default query bounds.
2. Add paginated `GET /api/billing` control-plane endpoint with stable cursor semantics and default query bounds.
3. Both endpoints support filter combinations for project, model, provider, and date range.
4. Add integration tests covering pagination cursors and filter combinations for both endpoints.
5. Ensure OpenAPI schema matches implementation and regenerate generated API types.

## Validation
- `make test`
- `make coverage`
- `make ci`

## Progress Notes
- 2026-02-28: Started implementation on branch `codex/ui-m-041-usage-billing-dashboard`.
