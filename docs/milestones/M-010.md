# Milestone M-010

## Source
- Issue: #16
- Area: area/billing

## Acceptance Criteria
1. `GET /api/orgs/:orgId/usage` supports `from`, `to`, and `group_by` query params and returns OpenAPI-compatible response shape.
2. Endpoint returns aggregated metrics (requests, tokens, cost, platform fee) from fixture-backed repository.
3. Validation errors use shared error envelope format with `request_id`.
4. Tests cover valid query, invalid query params, and grouping mode (`hour` and `model` via route/unit tests).
5. `make test`, `make coverage`, and `make ci` pass.

## Scope
- In scope: fixture-backed usage reporting endpoint, aggregation logic, tests, OpenAPI contract update.
- Out of scope: DB-backed query implementation, streaming usage feeds, reconciliation UI.

## Test Plan
- Unit: query validation and model grouping aggregation.
- Integration: endpoint valid query + invalid query params.
- CI: `make test`, `make coverage`, `make ci`.

## Verification Commands
```bash
make test
make coverage
make ci
```

## Status
- [x] Implemented
- [x] Tests added/updated
- [x] Gates green
- [ ] PR opened
