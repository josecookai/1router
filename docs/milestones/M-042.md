# M-042 Provider SLI and Incident Observability APIs

## Status
- in_review

## Scope
- Expose provider SLI snapshot API for incident dashboard consumption.
- Add routing decision trace list API with safe redaction constraints.
- Keep response contracts stable and OpenAPI-synchronized.

## Acceptance Criteria
- `GET /api/infra/slo` returns SLI snapshot with success/error/p95 and request count fields.
- `GET /api/infra/traces` returns route decision trace list for debugging requests.
- Trace responses redact sensitive prompt/input payload fields by default.
- Integration tests validate response schema and redaction behavior.
- `make test`, `make coverage`, `make ci` pass.

## Validation Plan
- `make test`
- `make coverage`
- `make ci`
