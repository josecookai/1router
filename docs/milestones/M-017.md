# Milestone M-017

## Source
- Issue: #37
- Area: area/router

## Acceptance Criteria
1. `POST /v1/responses` 对必填字段（`model`, `input`）执行最小必要校验，畸形 payload 返回 400。
2. 错误响应保持共享 envelope：`error.code`, `error.message`, `error.request_id`。
3. happy path 响应维持当前 MVP OpenAI-style 兼容结构。
4. 路由测试覆盖 happy path、invalid payload、unsupported model 三种路径。
5. `make test`, `make coverage`, `make ci` 全绿，覆盖率满足阈值。

## Scope
- In scope: `/v1/responses` 非流式校验强化与错误映射一致性、相关测试与契约同步。
- Out of scope: streaming responses、tool runtime、真实 provider 调用。

## Test Plan
- Route: valid payload -> 200
- Route: invalid payload -> 400 shared envelope
- Route: unsupported model -> 400 shared envelope
- CI: `make test`, `make coverage`, `make ci`

## Verification Commands
```bash
make test
make coverage
make ci
```

## Status
- [x] Implemented
- [x] Tests added/updated
- [x] Gates green
- [x] PR opened
