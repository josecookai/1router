# Milestone M-035

## Source
- Issue: #74
- Area: area/router

## Acceptance Criteria
1. `/v1/chat/completions` 支持 `stream=true`，返回 OpenAI 风格 SSE chunk 格式。
2. stream 请求在客户端断开后停止继续发送 token/chunk。
3. 测试覆盖 stream happy path 与 disconnect handling。
4. `make test`, `make coverage`, `make ci` 全绿并满足覆盖率门槛。

## Scope
- In scope: SSE chunk stub relay、disconnect stop、测试、OpenAPI/types 同步。
- Out of scope: provider 原生 streaming 透传、backpressure 优化。

## Verification Commands
```bash
make test
make coverage
make ci
```

## Status
- [x] Implemented
- [x] Tests added/updated
- [x] Gates green
- [ ] PR opened
