# Milestone M-001

## Source
- Issue: #2
- Area: area/router

## Acceptance Criteria
1. `GET /v1/models` returns HTTP 200 with OpenAI-compatible `{"object":"list","data":[...]}` response shape.
2. Returned models include capability metadata in `x_router.capabilities` (`tools`, `vision`, `json_schema`).
3. `openapi/mvp.yaml` documents `/v1/models` response schema and implementation tests validate the same payload shape via shared Zod contract.
4. Unit/integration tests pass via `make test` and `make ci`.

## Scope
- In scope: static in-memory catalog fixture, route handler, schema contract file, tests.
- Out of scope: DB-backed catalog, provider sync/polling, pricing freshness, live health metrics.

## Test Plan
- Unit: `test/models-catalog.test.ts` validates formatter output and router extension capabilities.
- Integration: `test/models-route.test.ts` validates Fastify `GET /v1/models` response and shared contract parsing.
- Manual: `npm run dev` then `curl http://localhost:3000/v1/models`.

## Verification Commands
```bash
make test
make coverage
make ci
```

## Status
- [x] Implemented
- [x] Tests added/updated
- [x] Gates green
- [x] PR opened
