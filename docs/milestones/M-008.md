# Milestone M-008

## Source
- Issue: #9
- Area: area/ui

## Acceptance Criteria
1. `GET /api/models` returns control-plane model catalog shape matching `openapi/mvp.yaml`.
2. `POST /api/policies` and `GET /api/policies` work with in-memory storage.
3. Validation failures return shared error envelope shape with `request_id`.
4. Policy responses include `weights`, `fallback_chain`, `constraints`, and `status`.
5. `make test`, `make coverage`, and `make ci` pass.

## Scope
- In scope: in-memory control-plane endpoints, validation hardening, tests, OpenAPI updates.
- Out of scope: DB persistence, routing engine enforcement, auth/RBAC.

## Test Plan
- Integration: `GET /api/models`, `POST /api/policies`, `GET /api/policies`.
- Validation: malformed policy payload returns `400` error envelope.
- CI: `make test`, `make coverage`, `make ci`.

## Verification Commands
```bash
make test
make coverage
make ci
```

## Status
- [x] Implemented
- [x] Tests added/updated
- [x] Gates green
- [ ] PR opened
